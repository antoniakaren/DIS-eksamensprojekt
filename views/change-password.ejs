<!DOCTYPE html>
<html lang="da">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Skift adgangskode</title>
  <link rel="stylesheet" href="/styles.css" />
</head>

<body>
  <main class="container">
    <h1>Skift adgangskode</h1>

    <!-- viser fejlbesked hvis controlleren har sendt 'error' med -->
    <% if (error) { %>
      <div class="alert alert-error"><%= error %></div>
    <% } %>

    <!-- viser succesbesked -->
    <% if (msg) { %>
      <div class="alert alert-success"><%= msg %></div>
    <% } %>

    <!-- POST-formular der sender gammel og nyt password til serveren -->
    <form action="/users/change-password" method="POST" class="form">
      <label>Gammelt password</label>
      <input type="password" name="oldPassword" required />

      <label>Nyt password</label>
      <input type="password" name="newPassword" required />

      <button type="submit">Opdater</button>
    </form>

    <!-- link tilbage til feed -->
    <p><a href="/feed">Tilbage til feed</a></p>
  </main>
</body>
</html>
