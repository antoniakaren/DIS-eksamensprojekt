<!DOCTYPE html>
<html lang="da">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Opret virksomhed</title>
  <link rel="stylesheet" href="/styles.css" />
</head>

<body>
  <main class="container">
    <h1>Opret virksomhed</h1>

    <!-- viser fejlbesked, fx ugyldigt password eller brugernavn der allerede findes -->
    <% if (error) { %>
      <div class="alert alert-error"><%= error %></div>
    <% } %>

    <!-- viser succes- eller informationsbeskeder fra controlleren -->
    <% if (msg) { %>
      <div class="alert alert-success"><%= msg %></div>
    <% } %>

    <!-- Formular til at oprette ny virksomhedskonto -->
    <!-- POST'er til signup-controlleren, hvor validering + hashing + email-kryptering sker -->
    <form action="/users/signup" method="POST" class="form">

      <label>Virksomhedsnavn</label>
      <input type="text" name="name" required />

      <label>Brugernavn</label>
      <input type="text" name="username" required />

      <label>Virksomheds-e-mail</label>
      <input type="email" name="email" required />

      <label>Kodeord</label>
      <input type="password" name="password" required />

      <button type="submit">Opret virksomhed</button>
    </form>

    <!-- Link tilbage til login hvis virksomheden allerede har en konto -->
    <p>Har virksomheden allerede en konto?  
      <a href="/users/login">Log ind</a>
    </p>

  </main>
</body>
</html>
