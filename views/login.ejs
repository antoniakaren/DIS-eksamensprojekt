<!DOCTYPE html>
<html lang="da">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Virksomhedslogin</title>
  <link rel="stylesheet" href="/styles.css" />
</head>

<body>
  <main class="container">
    <h1>Virksomhedslogin</h1>

    <!-- viser fejlbesked hvis login fejler -->
    <% if (error) { %>
      <div class="alert alert-error"><%= error %></div>
    <% } %>

    <!-- viser info eller succesbesked, hvis controller sætter 'msg' -->
    <% if (msg) { %>
      <div class="alert alert-success"><%= msg %></div>
    <% } %>

    <!-- Loginformular -->
    <form action="/users/login" method="POST" class="form">

      <label>Brugernavn</label>
      <input type="text" name="username" required />

      <label>Kodeord</label>
      <input type="password" name="password" required />

      <button type="submit">Log ind</button>
    </form>

    <!-- link til oprettelse af ny virksomhedskonto -->
    <p>Ingen virksomhedsprofil? 
      <a href="/users/signup">Opret virksomhed</a>
    </p>

  </main>
</body>
</html>
